name: nativeconnect
description: "The most intuitive Flutter package for seamless native feature integration."
version: 0.0.1
repository: https://github.com/anit/nativeconnect

environment:
  sdk: ^3.11.5
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^12.0.1
  geolocator: ^14.0.1
  image_picker: ^1.2.2
  device_info_plus: ^13.1.0
  share_plus: ^13.1.0
  sensors_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter: